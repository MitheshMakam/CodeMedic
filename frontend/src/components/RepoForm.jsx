import { useState } from 'react'
import { GitBranch, Sparkles } from 'lucide-react'
export default function RepoForm({ onSubmit, loading, label = 'Improve everything', initialUrl = '' }) { const [url, setUrl] = useState(initialUrl); const submit = e => { e.preventDefault(); const normalized = url.trim().replace(/^github\.com\//, 'https://github.com/'); if (normalized) onSubmit(normalized) }; return <form onSubmit={submit} className="glass flex flex-col gap-3 rounded-2xl p-3 sm:flex-row"><div className="flex flex-1 items-center gap-3 rounded-xl bg-black/20 px-4"><GitBranch className="text-slate-400" size={19}/><input value={url} onChange={e => setUrl(e.target.value)} required placeholder="https://github.com/owner/repository" className="h-12 w-full bg-transparent text-sm outline-none placeholder:text-slate-500" /></div><button disabled={loading} className="button flex items-center justify-center gap-2 px-5 py-3 disabled:opacity-60"><Sparkles size={17}/>{loading ? 'Reviewing…' : label}</button></form> }
