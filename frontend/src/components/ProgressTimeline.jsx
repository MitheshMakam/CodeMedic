import { CheckCircle2, LoaderCircle, Circle } from 'lucide-react'
const labels = { analyze:'Analyze repository', summary:'Summarize architecture', bugs:'Find bugs', security:'Security review', readme:'Generate README', tests:'Generate tests', improve:'Suggest improvements' }
export default function ProgressTimeline({ events }) { return <aside className="glass rounded-2xl p-5"><h2 className="font-semibold">Agentic pipeline</h2><div className="mt-4 space-y-4">{Object.entries(labels).map(([key, name]) => { const event = events.find(x => x.step === key); const running = event?.status === 'running'; return <div className="flex gap-3 text-sm" key={key}>{event?.status === 'completed' ? <CheckCircle2 size={19} className="shrink-0 text-emerald-400"/> : running ? <LoaderCircle size={19} className="shrink-0 animate-spin text-violet-400"/> : <Circle size={19} className="shrink-0 text-slate-600"/>}<div><p className={event ? 'text-slate-100' : 'text-slate-500'}>{name}</p>{running && <p className="text-xs text-slate-500">{event.message}</p>}</div></div> })}</div></aside> }
