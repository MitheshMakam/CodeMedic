import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import { AlertTriangle, ShieldCheck } from 'lucide-react'
const colors = { critical:'bg-rose-500/20 text-rose-300', high:'bg-orange-500/20 text-orange-300', medium:'bg-amber-500/20 text-amber-300', low:'bg-sky-500/20 text-sky-300', info:'bg-slate-500/20 text-slate-300' }
export default function ReportCard({ report }) { return <article className="glass rounded-2xl p-6"><div className="mb-4 flex items-start justify-between gap-4"><div><h2 className="text-xl font-semibold">{report.title}</h2><p className="mt-1 text-sm text-slate-400">{report.overview}</p></div>{report.ai_enriched ? <ShieldCheck className="text-emerald-400"/> : <AlertTriangle className="text-amber-400"/>}</div><div className="markdown"><ReactMarkdown remarkPlugins={[remarkGfm]}>{report.markdown}</ReactMarkdown></div>{report.findings?.length > 0 && <div className="mt-5 space-y-2">{report.findings.map((item, index) => <div key={`${item.title}-${index}`} className="rounded-xl border border-white/5 bg-black/15 p-3 text-sm"><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full px-2 py-0.5 text-xs font-medium ${colors[item.severity]}`}>{item.severity}</span><b>{item.title}</b>{item.path && <code className="text-slate-400">{item.path}{item.line ? `:${item.line}` : ''}</code>}</div><p className="mt-1 text-slate-400">{item.recommendation}</p></div>)}</div>}</article> }
